describe('RatingService (minimal unit test)', () => {

  it('should create a new rating (when none exists) and update article sums', async () => {
    // TODO: write test that verifies behaviour when making new rating
  });

  it('should throw exception if the article does not exist', async () => {
    // TODO: write test that verifies intended behaviour on missing article
  });
});